services:
  db:
    image: postgres:17
    healthcheck:
      test: pg_isready -U quarkus -d quarkus
      interval: 5s
      timeout: 3s
      retries: 3
    ports:
      - "55432:5432"
    environment:
      POSTGRES_USER: quarkus
      POSTGRES_PASSWORD: quarkus
      POSTGRES_DB: quarkus
